// GHShieldGenePoolAttributes - removed.
//
// There is no Gene Pool type in Grasshopper. Checking Grasshopper.dll for
// Rhino 7 shows no GH_GenePool or GH_GenePoolAttributes at all: the Gene Pool
// is part of Galapagos, which ships as a separate assembly. Protecting it
// would mean referencing Galapagos as well, which is a decision worth taking
// deliberately rather than by accident.
//
// This file is intentionally empty so it compiles. It can be deleted from the
// project whenever convenient.
